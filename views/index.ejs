<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sensor Readings</title>
  <script src="/socket.io/socket.io.js"></script>
  <script>
    const socket = io();
    
    socket.on('sensorData', (data) => {
      document.getElementById('sensorData').innerHTML = JSON.stringify(data, null, 2);
    });
  </script>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 20px;
    }
    pre {
      background-color: #34f8eb;
      padding: 10px;
      border-radius: 5px;
    }
  </style>
</head>
<body>
  <h1>Sensor Data</h1>
  <h2>Live Readings from MQTT Broker</h2>
  <pre id="sensorData"><%= JSON.stringify(message, null, 2) %></pre>
  
</body>
</html>